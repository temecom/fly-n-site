
<EntityListComponent  
@   items={{this.model.memberships}} 
    @title={{t "site.memberships"}} 
    @itemSelected={{this.membershipSelected}} 
    @newEntity={{this.newMembership}}></EntityListComponent>

{{outlet}}