<EntityListComponent 
    @title={{t "site.sites"}}
    @items={{this.model.sites}} 
    @itemSelected={{this.siteSelected}}
    @createNewEntity={{this.newSite}}
>
</EntityListComponent>

{{outlet}}
